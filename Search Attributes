Public Function Attr(arr As Range, c As Range) As String
   Dim rng As Range
   Dim Str As String
   Str = ""
   For Each rng In arr
       If c Like "*" & rng & "*" Then
           Str = Str & rng.Value & "| "
       End If
   Next
   Attr = Str
End Function
